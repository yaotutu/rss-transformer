{
  "name": "rss-transformer",
  "version": "0.0.1",
  "description": "RSS Transformer",
  "author": "yaotutu",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "bdev": "pnpm run --prefix packages/backend start:dev",
    "bm": "pnpm run --prefix packages/backend migrate",
    "bg": "pnpm run --prefix packages/backend generate",
    "fdev": "pnpm run --prefix packages/frontend dev"
  }
}
